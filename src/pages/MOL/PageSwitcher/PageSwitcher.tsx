import Box from '@mui/material/Box';

function PageSwitcher() {
  return (
    <Box
      sx={{
        position: 'fixed',
        left: 0,
        display: 'flex',
        height: '100%',
        alignItems: 'center',
        zIndex: 2,
      }}
    >
      **
    </Box>
  );
}

export default PageSwitcher;
